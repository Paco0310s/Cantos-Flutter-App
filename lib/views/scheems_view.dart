import 'package:flutter/material.dart';

class ScheemsView extends StatelessWidget {
  const ScheemsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('ScheemsView'));
  }
}
